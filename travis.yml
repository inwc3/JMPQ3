language: java
jdk:
  - oraclejdk8

before_install:
 - chmod +x gradlew

after_success:
 - ./gradlew jacocoTestReport coveralls
 
cache:
  directories:
    - $HOME/.gradle/wrapper
    - $HOME/.gradle/caches/modules-2/files-2.1


